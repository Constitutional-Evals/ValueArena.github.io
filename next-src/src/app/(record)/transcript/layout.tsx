import { pageMetadata } from '@/lib/metadata';

export const metadata = pageMetadata("Evaluation Transcript \u2014 ValueArena", "Scenario responses and model judgments from a published EigenBench evaluation.");

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
