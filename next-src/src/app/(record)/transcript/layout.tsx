import { pageMetadata } from '@/lib/metadata';

export const metadata = pageMetadata("Evaluation Transcript \u2014 ValueArena", "The scenarios, model answers and judgments from a published EigenBench run.");

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
