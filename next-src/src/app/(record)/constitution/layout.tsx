import { pageMetadata } from '@/lib/metadata';

export const metadata = pageMetadata("Constitution \u2014 ValueArena", "Evaluation principles used to judge language model responses in EigenBench.");

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
