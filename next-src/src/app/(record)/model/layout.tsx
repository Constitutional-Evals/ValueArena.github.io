import { pageMetadata } from '@/lib/metadata';

export const metadata = pageMetadata("Model Profile \u2014 ValueArena", "Language model details and published EigenBench evaluation results.");

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
