import { pageMetadata } from '@/lib/metadata';

export const metadata = pageMetadata("Constitution \u2014 ValueArena", "The written values that judges use to score model answers in EigenBench.");

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
