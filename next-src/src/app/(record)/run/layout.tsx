import { pageMetadata } from '@/lib/metadata';

export const metadata = pageMetadata("Experiment Results \u2014 ValueArena", "Scores, settings and judgments from one EigenBench run.");

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
